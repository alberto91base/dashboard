import styled, { css } from 'styled-components';
import Menu from './Menu';

export default styled(Menu).attrs({})`
    display: flex;
    background: #cfdad9;
    .menuContent {
        flex: 1;
    }
`;
